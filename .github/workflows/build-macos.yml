name: Build macOS Apps (Separate Architectures)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

# 可选：添加环境变量作为保险
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-x86_64:
    name: Build x86_64 (Intel) Version
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5  # 升级到 v5，支持 Node.js 24

      - name: Setup Python for x86_64
        uses: actions/setup-python@v6  # 升级到 v6，支持 Node.js 24
        with:
          python-version: '3.13'

      - name: Install dependencies and build for x86_64
        run: |
          arch -x86_64 /bin/bash -c "
            python -m venv venv_x86
            source venv_x86/bin/activate
            pip install --upgrade pip setuptools wheel
            if [ -f requirements.txt ]; then
              pip install -r requirements.txt
            else
              pip install PyInstaller PySide6 aiohttp
            fi
            python -m PyInstaller \
              --windowed \
              --name CloudflareScan_x86_64 \
              --icon=cfs.icns \
              --add-data 'cfs.icns:.' \
              --hidden-import=asyncio \
              --hidden-import=aiohttp \
              --hidden-import=PySide6.QtCore \
              --hidden-import=PySide6.QtGui \
              --hidden-import=PySide6.QtWidgets \
              cfs-mac.py
          "

      - name: Create DMG for x86_64
        run: |
          brew install create-dmg
          create-dmg \
            --volname "CloudflareScan (Intel)" \
            --volicon "cfs.icns" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --icon "CloudflareScan_x86_64.app" 200 190 \
            --hide-extension "CloudflareScan_x86_64.app" \
            --app-drop-link 600 185 \
            "CloudflareScan_Intel.dmg" \
            "dist/"
          # 删除不再需要的 .app 文件夹
          rm -rf dist

      - name: Upload x86_64 artifacts
        uses: actions/upload-artifact@v6  # 升级到 v6，原生支持 Node.js 24
        with:
          name: CloudflareScan-x86_64
          path: CloudflareScan_Intel.dmg   # 只上传 dmg 文件
          retention-days: 7

  build-arm64:
    name: Build arm64 (Apple Silicon) Version
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5  # 升级到 v5

      - name: Setup Python for arm64
        uses: actions/setup-python@v6  # 升级到 v6
        with:
          python-version: '3.13'

      - name: Install dependencies and build for arm64
        run: |
          python -m venv venv_arm
          source venv_arm/bin/activate
          pip install --upgrade pip setuptools wheel
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install PyInstaller PySide6 aiohttp
          fi
          python -m PyInstaller \
            --windowed \
            --name CloudflareScan_arm64 \
            --icon=cfs.icns \
            --add-data 'cfs.icns:.' \
            --hidden-import=asyncio \
            --hidden-import=aiohttp \
            --hidden-import=PySide6.QtCore \
            --hidden-import=PySide6.QtGui \
            --hidden-import=PySide6.QtWidgets \
            cfs-mac.py

      - name: Create DMG for arm64
        run: |
          brew install create-dmg
          create-dmg \
            --volname "CloudflareScan (Apple Silicon)" \
            --volicon "cfs.icns" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --icon "CloudflareScan_arm64.app" 200 190 \
            --hide-extension "CloudflareScan_arm64.app" \
            --app-drop-link 600 185 \
            "CloudflareScan_AppleSilicon.dmg" \
            "dist/"
          # 删除不再需要的 .app 文件夹
          rm -rf dist

      - name: Upload arm64 artifacts
        uses: actions/upload-artifact@v6  # 升级到 v6
        with:
          name: CloudflareScan-arm64
          path: CloudflareScan_AppleSilicon.dmg   # 只上传 dmg 文件
          retention-days: 7

  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-x86_64, build-arm64]
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download both artifacts
        uses: actions/download-artifact@v6  # 升级到 v6
        with:
          path: release-artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2  # 升级到 v2，支持 Node.js 24
        with:
          files: |
            release-artifacts/CloudflareScan-x86_64/CloudflareScan_Intel.dmg
            release-artifacts/CloudflareScan-arm64/CloudflareScan_AppleSilicon.dmg
          draft: false
          prerelease: false
